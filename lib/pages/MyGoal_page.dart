import 'package:flutter/material.dart';

// Gunakan PascalCase untuk nama class (MyGoalPage)
class MyGoal_page extends StatelessWidget {
  const MyGoal_page({super.key});

  @override
  Widget build(BuildContext context) {


    return const Center(
      child: Text(
        'Ini Halaman My Goal',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
