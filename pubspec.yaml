name: tes
description: "YourGoal"


publish_to: 'none' # Remove this line if you wish to publish
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_native_splash: ^2.4.0
  cupertino_icons: ^1.0.8

  go_router: ^16.2.4
  google_fonts: ^6.2.1
  provider: ^6.1.2

  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter_native_splash:
  # Warna background di belakang gambar Anda
  color: "#5B94FF" # Ganti dengan kode hex warna Anda (misal: #000000 untuk hitam)

  # Path ke gambar splash screen Anda
  image: assets/images/splash.png
  android_gravity: fill


  android_12:
  # Ini adalah bagian khusus untuk Android 12+
  # Gunakan logo transparan yang sama
    image: assets/images/splash_logo.png

    # Samakan warna ini dengan 'color' di atas agar konsisten
    icon_background_color: "#5B94FF"

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
   - assets/images/
  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
